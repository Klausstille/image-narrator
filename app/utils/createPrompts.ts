export default function createPrompts({
    name,
    system,
    user,
}: {
    name: string;
    system: string;
    user: string;
}) {
    return {
        id: `aNygDRVyEgZgd4zRe1B2`,
        name: name,
        system: system,
        user: user,
    };
}
