export default function createPrompts({
    name,
    system,
    user,
}: {
    name: string;
    system: string;
    user: string;
}) {
    return {
        id: `6XEBM5aTN5QntkgHSTzt`,
        name: name,
        system: system,
        user: user,
    };
}
